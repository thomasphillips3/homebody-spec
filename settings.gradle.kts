// A minimal settings.gradle.kts is included alongside build.gradle.kts so
// this is a well-formed, single-module Gradle project for JitPack to build
// (JitPack's docs recommend a settings file be present even for single-module
// builds; without one, Gradle emits a root-project-name warning).
rootProject.name = "homebody-spec"
